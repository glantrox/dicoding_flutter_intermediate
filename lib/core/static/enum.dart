enum CurrentAuth { auth, login, register, success, guest }

enum CurrentLoggedIn { detail, addStory, storyList, wantToLogout }

enum AuthState { init, loading, error, success }

enum ApiState { init, loading, error, success }

enum Api2State { init, loading, error, success }
